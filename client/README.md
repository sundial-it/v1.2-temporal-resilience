Client (static)
================

This folder contains the current static site which is copied from the repository root into `public/` to be served by the server.

To serve locally:

```bash
cd client
npm install
npm run start
```

The server in `/server` will automatically serve `client/public` in production.
