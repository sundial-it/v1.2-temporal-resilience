/* Sandbox client copied into client public */
let encryptedMemory = ""; let baseStringMemory = "";
function generateDeterministicSeed(plaintext, bits, mode) { let seed = 0; const combinedStr = plaintext + mode + bits; for (let i = 0; i < combinedStr.length; i++) { seed = (seed ^ combinedStr.charCodeAt(i) * (i + 19)) & 0xFFFFFF; } return seed; }
function encryptAction(){ triggerBeep(440,0.08); const plain = document.getElementById('plain-input')?.value.trim(); const mode = document.getElementById('cipher-mode')?.value; const bits = document.getElementById('bit-depth')?.value; const logBox = document.getElementById('sandbox-log'); const badge = document.getElementById('operation-badge'); if(!plain){ alert('Plaintext required'); return; } logBox.innerHTML = "<span class='text-white animate-pulse'>GENERATING DETERMINISTIC SHADOW SEEDS...</span>"; badge.textContent = "ALIGNING..."; setTimeout(()=>{ const seed = generateDeterministicSeed(plain,bits,mode); baseStringMemory = plain; let intermediate = ""; for(let i=0;i<plain.length;i++){ const charCode = plain.charCodeAt(i); const scrambled = (charCode ^ (seed + i*43)) & 0xFF; intermediate += String.fromCharCode(scrambled); } const b64 = btoa(intermediate); let structuredOut = `SUNDIAL::${mode.toUpperCase()}::${bits}::`; const qSymbols = ['☼','☾','★','⌬','⚙','⧉','⏱','⎈']; for(let i=0;i<b64.length;i++){ structuredOut += b64[i]; if(i%5===0 && i>0){ structuredOut += qSymbols[(seed+i)%qSymbols.length]; } } structuredOut += "::END"; encryptedMemory = structuredOut; logBox.innerHTML = `<span class='text-[#00e676]'>SHADOW FORMULATION COMPLETE // TEMP-KEY LOADED:</span><br><span class='text-white select-all font-bold font-mono text-[0.68rem]'>${structuredOut}</span>`; badge.textContent = "SHADOW_LOCKED"; document.getElementById('entropy-metric-val').textContent = (14.2 + Math.random()*2.5).toFixed(4) + " Arc/ms"; },700); }
function decryptAction(){ if(!encryptedMemory){ alert('No cipher present'); return; } const logBox = document.getElementById('sandbox-log'); logBox.innerHTML = `DIAL BASE COORDINATES SYNCHRONIZED.<br>PLAIN RECOVERY RESOLVED:<br><span class='text-[#ffd54f] font-bold text-sm select-all font-mono'>"${baseStringMemory}"</span>`; }
